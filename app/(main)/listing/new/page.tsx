import { ListingForm } from "@/components/listing/listing-form";

export const metadata = {
  title: "List Property",
  description: "Create a new property listing on ZAMINDHAAR",
};

export default function NewListingPage() {
  return <ListingForm />;
}
